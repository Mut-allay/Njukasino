services:
  - type: web
    name: njuka-backend
    runtime: python
    rootDir: backend/njuka-webapp-backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.4
